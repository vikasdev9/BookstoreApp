📁 1. Go to project root
cd D:\GitHubRepository\BookstoreApp
📄 2. Create .gitignore

Create file: .gitignore in root

Paste this:

# Node
node_modules/

# Backend
backend/node_modules/

# Mobile
mobile/node_modules/

# Env files (IMPORTANT)
.env

# Expo / React Native
.expo/
.expo-shared/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Build
dist/
build/
🔧 3. Initialize Git
git init
📦 4. Add all files
git add .
💾 5. Commit
git commit -m "Initial commit - Bookstore App"
🌐 6. Create GitHub repo

Go to:
👉 https://github.com/new

Name: BookstoreApp
Create repo
🔗 7. Connect remote
git remote add origin https://github.com/YOUR_USERNAME/BookstoreApp.git
🔄 8. Pull remote (IMPORTANT)
git pull origin main --rebase
🚀 9. Push code
git branch -M main
git push -u origin main
⚠️ If push fails (common fix)
git push -u origin main --force

👉 Use only if repo is new

✅ FINAL STRUCTURE
BookstoreApp/
  backend/
  mobile/
  .gitignore
🔐 IMPORTANT (REMEMBER)

❌ NEVER push:

.env
JWT_SECRET
Cloudinary keys
Mongo URI
🧠 QUICK FLOW (SHORT VERSION)
git init
git add .
git commit -m "init"
git remote add origin <repo-url>
git pull origin main --rebase
git push -u origin main
💬 BONUS (UPDATE CODE LATER)

Next time:

git add .
git commit -m "update"
git push